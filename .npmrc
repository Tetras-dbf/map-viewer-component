# eslint-plugin-react and eslint-plugin-jsx-a11y declare peer dependencies up
# to eslint@^9 but work correctly with ESLint 10 via @eslint/compat
# fixupPluginRules(). Remove this once those plugins publish ESLint
# 10-compatible releases.
legacy-peer-deps=true
